import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.huy.todolist',
  appName: 'To Do List Web to Android',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
